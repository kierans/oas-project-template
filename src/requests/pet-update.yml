description: |
  Pet object that needs to be added to the store
content:
  application/json:
    schema:
      "$ref": "../schemas/pet.yml"
  application/xml:
    schema:
      "$ref": "../schemas/pet.yml"
  application/x-www-form-urlencoded:
    schema:
      "$ref": "../schemas/pet.yml"
